# Database Configuration (Defaults in docker-compose are fine for local dev)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=kasparro_etl
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# API Keys (Optional - Free tier used if empty)
# Get keys from: https://www.coingecko.com/en/api and https://coinpaprika.com/api/
COINGECKO_API_KEY=
COINPAPRIKA_API_KEY=

# Security
# Secret to protect the manual trigger endpoint (POST /admin/trigger-etl)
# If left empty, defaults to: default_insecure_secret
ADMIN_SECRET=default_insecure_secret
